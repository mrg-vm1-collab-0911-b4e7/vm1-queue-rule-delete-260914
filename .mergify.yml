pull_request_rules:
  - name: VM1 rebase-bot branch credential boundary
    conditions:
      - title ~= ^VM1_REBASE_BOT_
      - "#commits-behind > 0"
    actions:
      rebase:
        bot_account: livastic-vm1-primary
