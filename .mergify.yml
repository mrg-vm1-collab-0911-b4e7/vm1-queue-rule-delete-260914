defaults:
  actions:
    label:
      remove_all: true

pull_request_rules:
  - name: VM1 defaults false precedence probe
    conditions:
      - title = VM1_DEFAULTS_FALSE_PRECEDENCE
    actions:
      label:
        remove_all: false
        add:
          - vm1-defaults-marker
