merge_protections:
  - name: VM1 PR files 3000 external fork gate
    if:
      - title = VM1_PR_FILES_3001_EXTERNAL_FORK_AUTO
    success_conditions:
      - "#files = 3001"
      - -files = zzzz_vm1_security_target_3001.txt

merge_protections_settings:
  reporting_method: check-runs
  post_comment: false
  auto_merge_conditions: true
