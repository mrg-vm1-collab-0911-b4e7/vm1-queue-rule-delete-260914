pull_request_rules:
  - name: VM1 classic last-push count1 discriminator
    conditions:
      - title = VM1_CLASSIC_LASTPUSH_COUNT1
      - body = VM1_CLASSIC_LASTPUSH_RELEASE
    actions:
      merge:
        method: merge
        merge_bot_account: livastic-vm1-primary
