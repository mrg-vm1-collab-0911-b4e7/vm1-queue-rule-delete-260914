defaults:
  actions:
    label:
      add:
        - vm1-default-list-marker

pull_request_rules:
  - name: VM1 defaults empty-list precedence probe
    conditions:
      - title = VM1_DEFAULTS_EMPTY_LIST_PRECEDENCE
    actions:
      label:
        add: []
